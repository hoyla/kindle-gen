publishing {
  files {
    outputDir = "target/tmp"
  }
}

query {
  downloadTimeout = 10 minutes  // it takes a long time to download master images
}

KindleGeneratorSpec {
  deleteGeneratedFiles = true
}
