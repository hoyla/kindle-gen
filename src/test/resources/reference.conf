publishing {
  files {
    outputDir = "target/tmp"
  }

  prettifyXml = true

  images {
    download = on
  }
}

query {
  downloadTimeout = 1 minute
  maxImageResolution = 500
}

KindleGeneratorSpec {
  deleteGeneratedFiles = true
}
